import React from 'react';

const PageNotFound= () => {
    return (
        <div>
            <h1>
                ОШИБКА, ВЕРНУТСЯ НА ГЛАВНУЮ
            </h1>
        </div>
    );
};

export default PageNotFound;